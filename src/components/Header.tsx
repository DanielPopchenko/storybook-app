import { View, Text } from 'react-native';
import React from 'react';

// ? through Stack.Navigator ?
// ? Or just View with header styles ?

// const Header = () => {
//   return (
//     <Stack.Navigator>
//       <Stack.Screen name="Home" component={HomeScreen} options={{ title: 'My home' }} />
//     </Stack.Navigator>
//   );
// };

// export default Header;
